k = 1.0;
tau = 0.4;
tEnd = 5.0*tau;

y[t_] := k*(1 - Exp[-t/tau]);

Plot[y[t], {t, 0, tEnd},
  AxesLabel -> {"t", "y(t)"},
  PlotRange -> All,
  PlotLegends -> {"step response"}]

(* Solve for T_eps such that y(T_eps) = k*(1 - eps) *)
eps = 0.05;
T_eps = -tau*Log[eps]
